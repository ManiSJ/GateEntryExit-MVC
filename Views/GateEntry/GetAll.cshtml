@using GateEntryExit_MVC.Models.GateEntry
@using GateEntryExit_MVC.Models.Shared

@model GetAllGateEntriesDto

@{
    var totalItems = Model.TotalCount;
    var items = Model.Items;
}


<div class="container">
    <div class="row">
        <div class="col-6">
            <button class="btn btn-primary" onclick="showFormModal('@Url.Action("AddOrEdit", "GateEntry", null, Context.Request.Scheme)','New Gate Enty')">Create</button>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Actions</th>
                        <th>
                            Name
                        </th>
                        <th>
                            NumberOfPeople
                        </th>
                        <th>
                            TimeStamp
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var gateEntry in items)
                    {
                        <tr>
                            <td>
                                <button class="btn btn-primary">Edit</button>
                                <button class="btn btn-danger">Delete</button>
                            </td>
                            <td>@gateEntry.GateName</td>
                            <td>@gateEntry.NumberOfPeople</td>
                            <td>@gateEntry.TimeStamp</td>
                        </tr>
                    }
                </tbody>
            </table>
            @await Component.InvokeAsync("Pagination", new PaginationModel(){
                Controller = "GateEntry",
                Action = "GetAll",
                TotalItems = totalItems
            })
        </div>
    </div>
</div>
   
